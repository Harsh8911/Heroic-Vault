{
  "framework": "create-react-app",
  "installCommand": "npm ci",
  "buildCommand": "if git lfs version >/dev/null 2>&1; then git lfs pull && npm run verify:unity; else echo '[vercel] git lfs not available, skipping unity verification'; fi && npm run build",
  "outputDirectory": "build"
}
